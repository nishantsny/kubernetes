apiVersion: v1
kind: Pod
metadata:
  name: nginx-10
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
    resources:
        limits:
          cpu: "0.5"
          memory: "3G"
        softRequests:
          cpu: "0.2"
          memory: "250Mi"
